<%- include('../layout', { title: 'Digital Identity', content: `
  <div class="did-container">
    <div class="did-card">
      <h2>🌍 Your Digital Identity</h2>
      <p><strong>DID:</strong> <span id="did-text">1234 5678 9090</span></p>

        <button onclick="copyDID()" class="btn-copy">📋 Copy DID</button>
    </div>
  </div>

  <script>
    function copyDID() {
      const didText = document.getElementById('did-text').textContent;
      navigator.clipboard.writeText(didText).then(() => {
        alert('DID copied to clipboard!');
      });
    }
  </script>

  <style>
    .did-container {
      max-width: 600px;
      margin: auto;
      padding: 20px;
      text-align: center;
      font-family: 'Arial', sans-serif;
    }

    .did-card {
      background: #f8f9fa;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .btn-copy, .btn-generate {
      display: inline-block;
      margin-top: 15px;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      font-weight: bold;
    }

    .btn-copy {
      background: #28a745;
      color: white;
      border: none;
    }

    .btn-copy:hover {
      background: #218838;
    }

    .btn-generate {
      background: #007bff;
      color: white;
    }

    .btn-generate:hover {
      background: #0056b3;
    }
  </style>
` }) %>
